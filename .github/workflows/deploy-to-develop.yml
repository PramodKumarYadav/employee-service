name: 🟢 deploy-to-dev

on:
  push:
    branches: ["main"]  
  workflow_dispatch:

run-name: deploy ${{ github.event.repository.name }} to dev

jobs:
  deploy_service-to-dev:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      # Dummy steps just to demonstrate the concept
      - name: setup infra
        run: echo "Setting infrastructure" 
      
      - name: build application
        run: echo "Building application"
      
      - name: deploy application
        run: echo "App deployed" 
  
  notify:
    runs-on: ubuntu-latest
    name: Notify test repositories that deployment is completed on main env and thus they can run tests specific to this service.
    needs: deploy_service-to-dev
    strategy:
      matrix:
        repos: [ 'PramodKumarYadav/playwright-sandbox' ]
    steps:
      - name: Dispatch event to repository
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ matrix.repos }}
          event-type: ${{ github.event.repository.name }}-dev-deployed
          client-payload: '{ "github": ${{ toJson(github) }},"TEST_ENV": "dev" ,  "TAG_OF_TESTS_TO_RUN": "${{ github.event.repository.name }}"}'

